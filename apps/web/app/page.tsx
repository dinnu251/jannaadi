// Placeholder — frontend agent (Antigravity) replaces this. Backend owns app/api/* and app/healthz only.
export default function Home() {
  return <main>JanNaadi API — see handovers/API.md</main>;
}
